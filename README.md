# Nobel's Legacy

> Currently, the pirate is only able to dig a location where they are standing. The pirate has found a stick of dynamite that can be used to clear a 3x3 grid around them, as below:
> 
> XXX  
> XPX  
> XXX
> 
>  Implement this. 
> 
> - The pirate should only be able to use the dynamite once. 
> - They are able to use it at any location they can stand on. 
> - If any items are within the 3x3 grid, 'claim' them. 
> - If the treasure is within the 3x3 grid, the treasure is destroyed.

Modified program to add single-use dynamite that explodes in a 3x3 pattern.